<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verification <%= status === 'approved' ? 'Successful' : status === 'failed' ? 'Failed' : 'Status' %> | M'Cube Plus</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      background: linear-gradient(135deg, #fff8e6 0%, #f8e0b2 50%, #f2c46d 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
    .container {
      max-width: 520px;
      width: 100%;
      background: #ffffff;
      border-radius: 16px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.25);
      overflow: hidden;
      animation: slideUp 0.4s ease-out;
    }
    .logo-mark {
      width: 72px;
      height: auto;
      display: block;
      margin: 0 auto 12px;
    }
    @keyframes slideUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .result-icon {
      padding: 48px 24px 24px;
      text-align: center;
    }
    .icon-circle {
      width: 96px;
      height: 96px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 24px;
      font-size: 48px;
      animation: scaleIn 0.5s ease-out 0.2s both;
    }
    @keyframes scaleIn {
      from { transform: scale(0); }
      to { transform: scale(1); }
    }
    .success .icon-circle {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      box-shadow: 0 8px 24px rgba(16, 185, 129, 0.4);
    }
    .failed .icon-circle {
      background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
      box-shadow: 0 8px 24px rgba(239, 68, 68, 0.4);
    }
    .unknown .icon-circle {
      background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
      box-shadow: 0 8px 24px rgba(245, 158, 11, 0.4);
    }
    h1 {
      font-size: 1.875rem;
      font-weight: 700;
      margin-bottom: 12px;
      color: #111827;
    }
    .success h1 { color: #059669; }
    .failed h1 { color: #dc2626; }
    .unknown h1 { color: #d97706; }
    .subtitle {
      font-size: 1.0625rem;
      color: #6b7280;
      line-height: 1.6;
      padding: 0 24px;
    }
    .details {
      background: #f9fafb;
      padding: 24px;
      border-top: 1px solid #e5e7eb;
      border-bottom: 1px solid #e5e7eb;
      margin-top: 32px;
    }
    .details-title {
      font-weight: 600;
      color: #374151;
      margin-bottom: 16px;
      font-size: 0.9375rem;
    }
    .detail-row {
      display: flex;
      justify-content: space-between;
      padding: 10px 0;
      border-bottom: 1px solid #e5e7eb;
    }
    .detail-row:last-child {
      border-bottom: none;
    }
    .detail-label {
      font-weight: 500;
      color: #6b7280;
      font-size: 0.875rem;
    }
    .detail-value {
      font-weight: 600;
      color: #111827;
      font-size: 0.875rem;
      text-align: right;
    }
    .actions {
      padding: 24px;
    }
    .btn {
      width: 100%;
      padding: 14px 24px;
      border: none;
      border-radius: 8px;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.2s;
      text-decoration: none;
      display: block;
      text-align: center;
    }
    .btn-primary {
      background: linear-gradient(135deg, #b47b18 0%, #e3b23c 50%, #f7d36b 100%);
      color: #1f1305;
      box-shadow: 0 4px 12px rgba(180, 123, 24, 0.45);
    }
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(180, 123, 24, 0.55);
    }
    .info-box {
      background: #fff8e6;
      border: 1px solid #f3d08a;
      border-radius: 8px;
      padding: 16px;
      margin-top: 16px;
      font-size: 0.875rem;
      color: #5c3c0a;
      line-height: 1.5;
    }
    .failed .info-box {
      background: #fef2f2;
      border-color: #fecaca;
      color: #991b1b;
    }
    .status-text {
      font-weight: 600;
    }
    .status-text-approved { color: #059669; }
    .status-text-failed { color: #dc2626; }
    .status-text-pending { color: #d97706; }
  </style>
</head>
<body>
  <div class="container <%= status === 'approved' ? 'success' : status === 'failed' ? 'failed' : 'unknown' %>">
    <div class="result-icon">
      <img src="/assets/mcubePlus.png" alt="M'Cube Plus" class="logo-mark" />
      <div class="icon-circle">
        <% if (status === 'approved') { %>
          ✓
        <% } else if (status === 'failed') { %>
          ✗
        <% } else { %>
          ?
        <% } %>
      </div>
      
      <h1>
        <% if (status === 'approved') { %>
          Verification Successful!
        <% } else if (status === 'failed') { %>
          Verification Failed
        <% } else { %>
          Verification Status Unknown
        <% } %>
      </h1>
      
      <div class="subtitle">
        <% if (status === 'approved') { %>
          Your identity has been successfully verified. You can now access your M'Cube Plus account.
        <% } else if (status === 'failed') { %>
          We couldn't verify your identity. This might be due to unclear photo or mismatched information.
        <% } else { %>
          We couldn't retrieve your verification status. The session may have expired or is still processing.
        <% } %>
      </div>
    </div>

    <% if (name || email) { %>
    <div class="details">
      <div class="details-title">Verification Details</div>
      <% if (name) { %>
      <div class="detail-row">
        <span class="detail-label">Name</span>
        <span class="detail-value"><%= name %></span>
      </div>
      <% } %>
      <% if (email) { %>
      <div class="detail-row">
        <span class="detail-label">Email</span>
        <span class="detail-value"><%= email %></span>
      </div>
      <% } %>
      <div class="detail-row">
        <span class="detail-label">Status</span>
        <span class="detail-value status-text <%= status === 'approved' ? 'status-text-approved' : status === 'failed' ? 'status-text-failed' : 'status-text-pending' %>">
          <%= status === 'approved' ? 'Approved' : status === 'failed' ? 'Failed' : 'Pending' %>
        </span>
      </div>
    </div>
    <% } %>

    <div class="actions">
      <a href="https://mcubeholdings.com/" class="btn btn-primary">
       Visit Our Website
      </a>

      <% if (status === 'failed') { %>
      <div class="info-box">
        <strong>Need help?</strong> Common reasons for verification failure:
        <ul style="margin: 8px 0 0 20px;">
          <li>Photo quality (too dark, blurry, or obstructed)</li>
          <li>Ghana Card PIN doesn't match National ID</li>
          <li>Face not clearly visible in selfie</li>
        </ul>
        Please contact support if you need assistance.
      </div>
      <% } %>

      <% if (status === 'approved') { %>
      <div class="info-box">
        ✓ A confirmation email has been sent to your registered email address.
      </div>
      <% } %>
    </div>
  </div>
</body>
</html>
